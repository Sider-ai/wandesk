export default function CryptobotMachinePanel({ status }: { status?: unknown }) { return <pre className="rounded-[12px] border bg-white/70 p-3 text-[11px]">{JSON.stringify(status || {}, null, 2)}</pre>; }
